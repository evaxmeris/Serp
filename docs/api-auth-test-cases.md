# API 认证测试用例

**项目：** Trade ERP  
**版本：** v0.8.0  
**测试日期：** 2026-04-09  
**测试人员：** 张工（测试工程师）  

---

## 测试范围

1. ✅ 登录流程测试
2. ✅ API 认证测试（所有受保护 API）
3. ✅ 权限测试（不同角色：ADMIN / MANAGER / USER / VIEWER）
4. ✅ 性能测试（响应时间）

---

## 1. 登录流程测试用例

### 测试环境

- **服务地址：** `http://localhost:3001`
- **登录端点：** `POST /api/auth/login`

### 1.1 正常登录测试

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | LOGIN-001 |
| **测试场景** | 使用正确的邮箱和密码登录 |
| **前置条件** | admin 用户已存在 |
| **输入数据** | `{"email": "admin@trade-erp.com", "password": "Admin123!"}` |
| **预期结果** | 1. 返回 `success: true` <br> 2. 返回用户信息（包含 id, email, name, role）<br> 3. 设置 `auth-token` cookie |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

### 1.2 错误密码测试

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | LOGIN-002 |
| **测试场景** | 使用错误密码登录 |
| **前置条件** | admin 用户已存在 |
| **输入数据** | `{"email": "admin@trade-erp.com", "password": "wrong-password"}` |
| **预期结果** | 1. 返回 `success: false` <br> 2. 返回错误消息 "账号或密码错误" <br> 3. HTTP 状态码 401 |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

### 1.3 不存在用户测试

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | LOGIN-003 |
| **测试场景** | 登录不存在的用户 |
| **输入数据** | `{"email": "not-exists@example.com", "password": "any-password"}` |
| **预期结果** | 1. 返回 `success: false` <br> 2. 返回错误消息 "账号或密码错误" <br> 3. HTTP 状态码 401 |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

### 1.4 缺少必填字段测试

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | LOGIN-004 |
| **测试场景** | 缺少邮箱字段 |
| **输入数据** | `{"password": "Admin123!"}` |
| **预期结果** | 1. 返回错误 "邮箱和密码不能为空" <br> 2. HTTP 状态码 400 |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | LOGIN-005 |
| **测试场景** | 缺少密码字段 |
| **输入数据** | `{"email": "admin@trade-erp.com"}` |
| **预期结果** | 1. 返回错误 "邮箱和密码不能为空" <br> 2. HTTP 状态码 400 |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

### 1.5 登录频率限制测试

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | LOGIN-006 |
| **测试场景** | 连续 6 次失败登录，触发频率限制 |
| **前置条件** | IP 地址没有被限制 |
| **输入数据** | 连续 6 次错误密码尝试 |
| **预期结果** | 1. 前 5 次返回 401 <br> 2. 第 6 次返回 429 状态码 <br> 3. 返回 `retryAfter` 字段 |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

### 1.6 登出测试

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | LOGIN-007 |
| **测试场景** | 登录后调用登出 |
| **预期结果** | 1. 删除 `auth-token` cookie <br> 2. 登出后无法访问受保护 API |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

### 1.7 获取当前用户信息测试

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | LOGIN-008 |
| **测试场景** | 已登录时调用 `/api/auth/me` |
| **预期结果** | 1. 返回当前用户信息 <br> 2. 数据与登录时一致 |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | LOGIN-009 |
| **测试场景** | 未登录时调用 `/api/auth/me` |
| **预期结果** | 1. 返回 401 未认证 |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

---

## 2. API 认证测试用例

### 2.1 受保护 API 访问规则

所有业务 API 都需要认证。分类：

| API 分组 | 端点示例 | 认证要求 |
|----------|----------|----------|
| 客户管理 | `/api/customers` | 需要认证 |
| 订单管理 | `/api/orders` | 需要认证 |
| 产品管理 | `/api/products` | 需要认证 |
| 供应商管理 | `/api/suppliers` | 需要认证 |
| 仪表盘 | `/api/dashboard/*` | 需要认证 |
| 报价单 | `/api/quotations` | 需要认证 |
| 采购管理 | `/api/purchases` | 需要认证 |
| v1 API | `/api/v1/*` | 需要认证 |
| 健康检查 | `/api/health` | 不需要认证 |

### 2.2 未认证访问测试

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | AUTH-001 |
| **测试场景** | 不带 token 访问受保护 API |
| **测试步骤** | 对每个受保护 API 发送请求，不提供 token |
| **预期结果** | 1. 返回 `{success: false, error: '未认证，请先登录', code: 'UNAUTHORIZED'}` <br> 2. HTTP 状态码 401 |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

### 2.3 无效 token 测试

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | AUTH-002 |
| **测试场景** | 使用无效 token 访问受保护 API |
| **输入** | Authorization: Bearer `invalid-token-string` |
| **预期结果** | 1. 返回 401 <br> 2. 返回 UNAUTHORIZED 错误 |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

### 2.4 过期 token 测试

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | AUTH-003 |
| **测试场景** | 使用过期 token 访问 |
| **预期结果** | 1. 返回 401 <br> 2. 返回 UNAUTHORIZED 错误 |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

### 2.5 正确 token 测试（Bearer 方式）

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | AUTH-004 |
| **测试场景** | 使用正确 token（Authorization Bearer）访问 |
| **预期结果** | 1. 返回 200 <br> 2. 返回正确的数据 |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

### 2.6 正确 token 测试（Cookie 方式）

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | AUTH-005 |
| **测试场景** | 使用正确 token（Cookie）访问 |
| **预期结果** | 1. 返回 200 <br> 2. 返回正确的数据 |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

---

## 3. 权限测试（基于角色的访问控制）

### 角色定义

| 角色 | 权限级别 | 说明 |
|------|----------|------|
| **ADMIN** | 最高 | 可以访问所有数据，管理用户 |
| **MANAGER** | 高级 | 可以查看所有数据，但不能管理用户 |
| **USER** | 普通 | 只能访问自己创建的数据 |
| **VIEWER** | 只读 | 只能查看自己的数据，不能修改 |

### 3.1 ADMIN 权限测试

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | PERM-001 |
| **角色** | ADMIN |
| **测试场景** | ADMIN 访问客户列表 |
| **预期结果** | 可以看到所有客户（不受 ownerId 限制） |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | PERM-002 |
| **角色** | ADMIN |
| **测试场景** | ADMIN 访问用户列表 `/api/users` |
| **预期结果** | 可以访问（用户管理需要 ADMIN） |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | PERM-003 |
| **角色** | ADMIN |
| **测试场景** | ADMIN 创建客户 |
| **预期结果** | 可以创建，`ownerId` 自动设为自己 |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

### 3.2 MANAGER 权限测试

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | PERM-011 |
| **角色** | MANAGER |
| **测试场景** | MANAGER 访问客户列表 |
| **预期结果** | 可以看到所有客户（不受 ownerId 限制） |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | PERM-012 |
| **角色** | MANAGER |
| **测试场景** | MANAGER 访问用户列表 |
| **预期结果** | 权限不足（403） |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

### 3.3 USER 权限测试

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | PERM-021 |
| **角色** | USER |
| **测试场景** | USER 访问客户列表 |
| **预期结果** | 只能看到 `ownerId = 自己` 的客户 |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | PERM-022 |
| **角色** | USER |
| **测试场景** | USER 创建客户 |
| **预期结果** | 可以创建，`ownerId` 自动设为自己 |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | PERM-023 |
| **角色** | USER |
| **测试场景** | USER 访问其他用户创建的客户 |
| **预期结果** | 不能看到（行级隔离生效） |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | PERM-024 |
| **角色** | USER |
| **测试场景** | USER 访问用户列表 |
| **预期结果** | 权限不足（403） |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

### 3.4 VIEWER 权限测试

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | PERM-031 |
| **角色** | VIEWER |
| **测试场景** | VIEWER 读取自己的客户 |
| **预期结果** | 可以读取 |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | PERM-032 |
| **角色** | VIEWER |
| **测试场景** | VIEWER 创建客户 |
| **预期结果** | 权限不足（403 或不允许操作） |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

### 3.5 行级隔离测试（ROW-LEVEL SECURITY）

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | PERM-041 |
| **测试场景** | USER-A 创建客户，USER-B 尝试访问 |
| **预期结果** | USER-B 在列表中看不到该客户 |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | PERM-042 |
| **测试场景** | ADMIN 可以看到所有用户的客户 |
| **预期结果** | ADMIN 能看到 USER-A 和 USER-B 的所有客户 |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

---

## 4. 性能测试用例

### 4.1 响应时间测试

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | PERF-001 |
| **测试场景** | 登录接口响应时间 |
| **测试次数** | 10 次 |
| **接受标准** | P95 < 200ms |
| **预期结果** | 平均响应 < 100ms |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

| 测试项 | 内容 |
|--------|------|
| **用例 ID** | PERF-002 |
| **测试场景** | 认证中间件 overhead |
| **测试次数** | 100 次 |
| **接受标准** | 认证检查 < 5ms |
| **预期结果** | JWT 验证 < 5ms |
| **实际结果** | |
| **状态** | ⏱️ 待执行 |

### 各 API 响应时间基准

| API | 接受标准（平均响应） |
|-----|---------------------|
| `/api/customers` | < 50ms |
| `/api/orders` | < 50ms |
| `/api/products` | < 50ms |
| `/api/dashboard/overview` | < 100ms |
| `/api/v1/inventory` | < 50ms |

---

## 附录：测试账号

| 角色 | 邮箱 | 密码 | 用户 ID |
|------|------|------|---------|
| ADMIN | admin@trade-erp.com | Admin123! | - |
| MANAGER | manager@trade-erp.com | Manager123! | - |
| USER | user@trade-erp.com | User123! | - |
| VIEWER | viewer@trade-erp.com | Viewer123! | - |

---

**文档版本：** 1.0  
**最后更新：** 2026-04-09  
**状态：** ✅ 准备执行

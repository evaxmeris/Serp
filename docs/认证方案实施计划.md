# Trade ERP 认证方案统一实施计划

**方案**: A - 简化 JWT 认证  
**开始日期**: 2026-03-23  
**预计完成**: 2026-03-25 (3 天)  
**版本**: v0.7.0

---

## 👥 **团队分工**

### 项目经理 (PM) - AI 协调
**职责**:
- ✅ 制定实施计划
- ✅ 分配任务
- ✅ 进度跟踪
- ✅ 风险控制
- ✅ 质量保证

**当前任务**:
- [x] 制定实施计划
- [ ] 召开项目启动会
- [ ] 每日站会同步进度

---

### 架构师 - AI 技术负责人
**职责**:
- ✅ 技术方案设计
- ✅ 代码审查
- ✅ 技术难点攻关
- ✅ 性能优化

**当前任务**:
- [ ] 设计统一的认证接口
- [ ] 审查关键代码
- [ ] 解决技术难题

---

### 开发工程师 - AI 开发团队
**职责**:
- ✅ 代码实现
- ✅ 单元测试
- ✅ 代码优化

**当前任务**:
- [ ] 安装依赖
- [ ] 修改登录 API
- [ ] 修改认证 API
- [ ] 添加登出 API
- [ ] 更新中间件

---

### 测试工程师 - AI 测试团队
**职责**:
- ✅ 编写测试用例
- ✅ 功能测试
- ✅ 集成测试
- ✅ 回归测试

**当前任务**:
- [ ] 编写测试用例
- [ ] 执行测试
- [ ] 提交 bug 报告

---

## 📋 **实施步骤**

### 阶段 1: 准备工作 (Day 1 上午)

#### 任务 1.1: 安装依赖
**负责人**: 开发工程师  
**预计时间**: 10 分钟

```bash
cd /Users/apple/clawd/trade-erp
npm install jose
```

**验收标准**:
- [ ] `jose` 已安装到 `package.json`
- [ ] 可以正常导入 `jose` 模块

---

#### 任务 1.2: 代码审查
**负责人**: 架构师  
**预计时间**: 30 分钟

**审查内容**:
- [ ] 审查 `auth-simple.ts` 的实现
- [ ] 确认 JWT 生成和验证逻辑正确
- [ ] 确认 cookie 设置参数正确
- [ ] 确认 SECRET 密钥安全

---

### 阶段 2: 核心功能开发 (Day 1 下午 - Day 2)

#### 任务 2.1: 修改登录 API
**负责人**: 开发工程师  
**预计时间**: 1 小时

**文件**: `src/app/api/auth/login/route.ts`

**修改内容**:
```typescript
import { login } from '@/lib/auth-simple';

export async function POST(request: Request) {
  const body = await request.json();
  const { email, password } = body;
  
  // 调用简化认证
  const result = await login(email, password);
  
  if (result.success) {
    return NextResponse.json({
      success: true,
      user: result.user,
    });
  } else {
    return NextResponse.json(
      { error: result.error },
      { status: 401 }
    );
  }
}
```

**验收标准**:
- [ ] 登录成功返回用户信息
- [ ] 登录失败返回 401 错误
- [ ] 正确设置 auth-token cookie

---

#### 任务 2.2: 修改 /api/auth/me
**负责人**: 开发工程师  
**预计时间**: 30 分钟

**文件**: `src/app/api/auth/me/route.ts`

**修改内容**:
```typescript
import { getCurrentUser } from '@/lib/auth-simple';

export async function GET() {
  const user = await getCurrentUser();
  
  if (user) {
    return NextResponse.json({
      authenticated: true,
      user: user,
    });
  } else {
    return NextResponse.json(
      { authenticated: false },
      { status: 401 }
    );
  }
}
```

**验收标准**:
- [ ] 已登录返回用户信息
- [ ] 未登录返回 401

---

#### 任务 2.3: 添加登出 API
**负责人**: 开发工程师  
**预计时间**: 30 分钟

**文件**: `src/app/api/auth/logout/route.ts` (新建)

**内容**:
```typescript
import { logout } from '@/lib/auth-simple';

export async function POST() {
  await logout();
  
  return NextResponse.json({
    success: true,
    message: 'Logout successful',
  });
}
```

**验收标准**:
- [ ] 登出清除 auth-token cookie
- [ ] 返回成功响应

---

#### 任务 2.4: 更新中间件
**负责人**: 开发工程师  
**预计时间**: 1 小时

**文件**: `src/lib/auth.ts`

**修改内容**:
```typescript
import { getCurrentUser } from '@/lib/auth-simple';

export async function getSession(request: NextRequest) {
  const user = await getCurrentUser();
  
  if (user) {
    return {
      user: {
        id: user.id,
        email: user.email,
        name: user.name,
        role: user.role as UserRole,
      }
    };
  }
  
  return null;
}
```

**验收标准**:
- [ ] 从 JWT 读取真实用户信息
- [ ] 移除假用户逻辑
- [ ] 权限验证正常

---

#### 任务 2.5: 添加速率限制
**负责人**: 开发工程师  
**预计时间**: 2 小时

**文件**: `src/lib/rate-limit.ts` (新建)

**内容**:
```typescript
import { NextRequest, NextResponse } from 'next/server';

const limitMap = new Map<string, { count: number; resetTime: number }>();

export function rateLimit(
  request: NextRequest,
  options: {
    limit: number;
    windowMs: number;
  }
): NextResponse | null {
  const ip = request.headers.get('x-forwarded-for') || 'unknown';
  const now = Date.now();
  
  const record = limitMap.get(ip) || { count: 0, resetTime: now + options.windowMs };
  
  if (now > record.resetTime) {
    record.count = 1;
    record.resetTime = now + options.windowMs;
  } else {
    record.count += 1;
  }
  
  limitMap.set(ip, record);
  
  if (record.count > options.limit) {
    return NextResponse.json(
      { 
        error: 'Too many requests',
        retryAfter: Math.ceil((record.resetTime - now) / 1000)
      },
      { status: 429 }
    );
  }
  
  return null;
}
```

**验收标准**:
- [ ] 登录失败 5 次锁定 15 分钟
- [ ] 返回 429 错误
- [ ] 显示重试时间

---

### 阶段 3: 测试验证 (Day 3)

#### 任务 3.1: 单元测试
**负责人**: 测试工程师  
**预计时间**: 2 小时

**测试用例**:
- [ ] 登录成功测试
- [ ] 登录失败测试
- [ ] 密码错误测试
- [ ] 用户不存在测试
- [ ] JWT 生成测试
- [ ] JWT 验证测试
- [ ] Cookie 设置测试
- [ ] 登出测试
- [ ] 速率限制测试

---

#### 任务 3.2: 集成测试
**负责人**: 测试工程师  
**预计时间**: 2 小时

**测试场景**:
- [ ] 完整登录流程
- [ ] 刷新页面保持登录
- [ ] API 认证检查
- [ ] 权限验证
- [ ] 登出流程

---

#### 任务 3.3: 回归测试
**负责人**: 测试工程师  
**预计时间**: 2 小时

**测试范围**:
- [ ] 现有 API 不受影响
- [ ] 现有页面正常显示
- [ ] 导航栏正常
- [ ] 审批流程正常
- [ ] 产品调研正常

---

#### 任务 3.4: 性能测试
**负责人**: 架构师  
**预计时间**: 1 小时

**测试内容**:
- [ ] JWT 生成性能
- [ ] JWT 验证性能
- [ ] Cookie 读写性能
- [ ] 并发登录测试

---

### 阶段 4: 代码审查与优化 (Day 3 下午)

#### 任务 4.1: 代码审查
**负责人**: 架构师  
**预计时间**: 2 小时

**审查清单**:
- [ ] 代码规范
- [ ] 错误处理
- [ ] 安全性
- [ ] 性能优化
- [ ] 注释完整

---

#### 任务 4.2: Bug 修复
**负责人**: 开发工程师  
**预计时间**: 根据测试结果

**修复内容**:
- [ ] 修复测试发现的 bug
- [ ] 优化性能问题
- [ ] 完善错误处理

---

#### 任务 4.3: 文档更新
**负责人**: 开发工程师  
**预计时间**: 1 小时

**更新内容**:
- [ ] API 文档
- [ ] 认证流程说明
- [ ] 部署指南
- [ ] 故障排查指南

---

## 📊 **进度跟踪**

### Day 1 (2026-03-23)

**上午**:
- [ ] 安装依赖
- [ ] 代码审查

**下午**:
- [ ] 修改登录 API
- [ ] 修改 /api/auth/me

**状态**: ⏳ 待开始

---

### Day 2 (2026-03-24)

**上午**:
- [ ] 添加登出 API
- [ ] 更新中间件

**下午**:
- [ ] 添加速率限制
- [ ] 开始测试

**状态**: ⏳ 待开始

---

### Day 3 (2026-03-25)

**上午**:
- [ ] 单元测试
- [ ] 集成测试

**下午**:
- [ ] 回归测试
- [ ] 代码审查
- [ ] Bug 修复
- [ ] 提交代码

**状态**: ⏳ 待开始

---

## ⚠️ **风险控制**

### 技术风险

| 风险 | 可能性 | 影响 | 应对措施 |
|------|--------|------|----------|
| jose 库兼容性问题 | 低 | 中 | 准备替代方案 |
| Cookie 设置失败 | 低 | 高 | 详细测试各浏览器 |
| JWT 验证失败 | 低 | 高 | 添加详细日志 |
| 性能问题 | 中 | 中 | 性能测试优化 |

### 应对措施

1. **每日站会** - 每天上午 10 点同步进度
2. **代码审查** - 每个任务完成后立即审查
3. **测试先行** - 先写测试用例再开发
4. **回滚方案** - 保留 v0.6.0 代码随时可回滚

---

## 🎯 **验收标准**

### 功能验收

- [ ] 登录成功设置 cookie
- [ ] 刷新页面保持登录
- [ ] `/api/auth/me` 返回正确用户
- [ ] 登出清除 cookie
- [ ] 速率限制生效
- [ ] 权限验证正常

### 性能验收

- [ ] 登录响应 < 500ms
- [ ] JWT 验证 < 50ms
- [ ] 并发 100 用户正常

### 安全验收

- [ ] 密码加密存储
- [ ] JWT 签名验证
- [ ] HttpOnly cookie
- [ ] 速率限制生效
- [ ] 无敏感信息泄露

---

## 📝 **提交物**

### 代码
- [ ] 修改的 API 文件
- [ ] 新建的中间件文件
- [ ] 测试文件

### 文档
- [ ] API 文档更新
- [ ] 认证流程说明
- [ ] 部署指南

### 测试报告
- [ ] 单元测试报告
- [ ] 集成测试报告
- [ ] 性能测试报告

---

**项目启动！各团队按计划执行！**

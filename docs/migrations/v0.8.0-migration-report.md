# Trade ERP v0.8.0 数据库迁移报告

**迁移时间：** 2026-03-25
**迁移名称：** v0.8.0_permission_system
**版本：** v0.8.0

## 迁移结果

✅ **迁移成功完成**

## 概要

- **原有表数量：** 46 个
- **新增表数量：** 21 个
- **总计表数量：** 67 个
- **索引总数：** 新增约 80+ 个复合索引和唯一约束

## 新增模块和表

### 1. 权限系统 - RBAC (5 表)

| 表名 | 说明 |
|------|------|
| `permissions` | 权限表 |
| `roles` | 角色表 |
| `user_roles` | 用户-角色关联表 |
| `role_permissions` | 角色-权限关联表 |
| `user_registrations` | 用户注册审批表 |

**设计要点：**
- 保留原有 `User.role` 单角色字段作为向后兼容
- 新增 RBAC 多角色多权限体系支持
- 支持用户注册审批流程

### 2. 多平台集成 (2 表)

| 表名 | 说明 |
|------|------|
| `platform_accounts` | 多平台账号配置 |
| `platform_orders` | 平台订单同步 |

**支持平台：** 阿里国际站、1688、亚马逊、TikTok Shop、Shopify、Wish、eBay

### 3. 海外仓管理 (2 表)

| 表名 | 说明 |
|------|------|
| `overseas_warehouses` | 海外仓配置 |
| `overseas_inventory` | 海外仓库存 |

**支持仓库类型：** FBA、FBN、自营、第三方

### 4. 审批流程 (3 表)

| 表名 | 说明 |
|------|------|
| `approval_flows` | 审批流程定义 |
| `approval_records` | 审批记录 |
| `approval_history` | 审批历史 |

**支持审批类型：** 用户注册、订单审批、采购审批、付款审批、报销审批、价格变更、折扣审批

### 5. 财务模块 (4 表)

| 表名 | 说明 |
|------|------|
| `accounts` | 账户表 |
| `transactions` | 交易记录 |
| `expenses` | 费用支出 |
| `reimbursements` | 报销单 |

**账户类型：** 现金、银行、支付宝、微信支付、PayPal、信用卡

### 6. 利润核算 (2 表)

| 表名 | 说明 |
|------|------|
| `profit_calculations` | 利润核算主表 |
| `profit_details` | 利润明细表（按订单/SKU）|

**功能：**
- 支持按月/季度/年度/自定义区间核算
- 自动分摊成本费用计算净利润
- 支持多平台统计

### 7. 绩效提成 (3 表)

| 表名 | 说明 |
|------|------|
| `performance_rules` | 绩效规则 |
| `performance_metrics` | 绩效指标统计 |
| `commissions` | 提成结算 |

**计算方式：** 固定金额、百分比、阶梯提成、佣金率

## 索引和约束检查

✅ **所有外键都添加了索引**
✅ **所有唯一约束正确配置**
✅ **常用查询条件添加复合索引**
  - `status` + `date` 范围查询
  - `userId` + `period` 按人员周期查询
  - `type` + `isActive` 按类型过滤激活项

## 向后兼容性

✅ **原有表结构保持不变**
✅ **没有删除任何原有字段**
✅ **原有 API 保持兼容**
✅ **所有新增表通过外键正确关联到原有表**
✅ **所有关系都支持级联删除**

## 迁移信息

- **迁移文件：** `prisma/migrations/20260324171013_v0_8_0_permission_system/migration.sql`
- **文件大小：** 87.8 KB
- **Prisma 版本：** 6.19.2
- **数据库：** PostgreSQL 已同步

## 下一步

- [x] 数据库迁移完成 ✓
- [ ] 执行代码审查
- [ ] 生成代码审查报告

**迁移工程师：** AI 架构师
**完成时间：** 2026-03-25 01:10 GMT+8

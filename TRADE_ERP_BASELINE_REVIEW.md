# Trade ERP 第一轮多角色审视 — 基线评估报告

**审查日期**：2026-05-02
**方法论**：multi-role-process-review（六角色审视法）
**项目版本**：v0.10.8
**代码规模**：78 Prisma 模型 · 125 API 路由 · 72 页面 · 2490 行 schema

---

## 📊 综合评分：56.4 / 100

| 角色 | 类型 | 评分 | 一句话发现 |
|------|------|:----:|-----------|
| 🧠 **人机交互设计师** | ⭐ 必选 | **68** | 核心流程完整但反馈不统一，alert/confirm 和 Toast 混用 |
| 🏗️ **系统架构师** | ⭐ 必选 | **58** | 4套认证系统并存，API 路径和响应格式碎片化，代码重复严重 |
| 👑 **管理员** | ⭐ 必选 | **62** | 配置页完整，但审批流/权限配置尚有缺口 |
| 🙋 **最终用户** | ⭐ 必选 | **65** | 日常操作功能完备，但交互粗糙、缺少人性化反馈 |
| 🧪 **外贸业务专家** | 知识域 | **67** | 数据模型扎实，但生产/质检/利润环节严重断裂 |
| 🔒 **安全工程师** | 知识域 | **45** | `/api/roles` 可提权，几乎无细粒度权限检查 |
| 📊 **数据分析师** | 知识域 | **45** | 报表页面完整但全部展示硬编码样本数据，无真实数据 |
| 💼 **业务操作员** | 用户群 | **62** | 日常下单/跟单流程可用，但操作效率有优化空间 |
| **平均** | | **56.4** | 系统可运行，但架构债+安全漏洞+业务流程断裂并存 |

---

## 🔴 P0 级问题（必须修复 — 阻塞运行或存在安全风险）

### 安全类

| # | 问题 | 严重性 | 位置 |
|---|------|--------|------|
| 1 | **`/api/roles` 权限提升漏洞** — 任何已登录用户可创建/修改角色，包括将自己提权为 ADMIN | 🔴 立即 | `/api/roles/route.ts` |
| 2 | **4 套认证系统共存** — auth-simple.ts / auth-api.ts / middleware/auth.ts / auth.ts 功能重叠，认证逻辑不一致导致安全策略无法统一 | 🔴 立即 | `src/lib/auth*.ts`, `src/middleware/auth.ts` |
| 3 | **所有写操作 API 无细粒度权限检查** — `requirePermission`/`withPermission` 存在但不被任何路由使用 | 🔴 本周 | 全部 125 个路由 |

### 业务流程类

| # | 问题 | 严重性 | 影响 |
|---|------|--------|------|
| 4 | **生产管理完全断裂** — ProductionRecord 无独立 API 和页面，只能读不能写 | 🔴 立即 | 订单→生产流程断链 |
| 5 | **质检管理完全断裂** — QualityCheck 无独立 API 和页面，检验记录无法录入 | 🔴 立即 | 质检→出货流程断链 |
| 6 | **利润核算为空壳** — 利润报表展示硬编码 sampleData，未接入真实 API | 🔴 立即 | 利润分析不可用 |
| 7 | **发票模块完全缺失** — 无 PI/CI 管理，外贸基本功能缺位 | 🔴 立即 | 无法向客户开票 |

### 架构类

| # | 问题 | 严重性 | 位置 |
|---|------|--------|------|
| 8 | **API 扁平路径与 v1 路径重复** — purchases 和 purchase-orders 都是同一业务，实现不同（返回格式/编号规则/权限检查均不一致） | 🔴 短期 | `/api/purchases/` vs `/api/v1/purchase-orders/` |
| 9 | **前端 alert()/confirm() 混用** — 订单页、新建订单页使用浏览器弹窗，客户页用 Toast，体验断裂 | 🔴 短期 | 订单页、新建订单页 |
| 10 | **取消订单确认后不执行 API** — confirmCancel() 只关对话框，不调接口 | 🔴 Bug | `src/app/orders/page.tsx:141-145` |

---

## 🟡 P1 级问题（重要 — 影响效率或体验）

### 交互体验
- 搜索无防抖（客户页、订单页每次按键发请求）
- 批量操作后 `window.location.reload()` 全页刷新，丢失滚动位置和筛选条件
- 无骨架屏加载态，全部页面用 spinner 或文本替代
- 仪表盘图表用 CSS div 手绘柱状图，无交互
- `shadcn Select` 与原生 `<select>` 混用
- 导航上下文丢失（"查看全部"不传递 filter 参数）

### 业务流程
- 询盘→报价无一键转换按钮
- 收款与订单状态无联动
- 质检与出货无衔接（QC PASSED 不触发状态流转）
- 佣金管理/绩效规则均无 UI 页面
- 订单状态机未嵌入质检前置条件检查

### 架构技术债
- 3 种 API 响应格式并存（有 success / 无 success / 仅 error）
- ErrorBoundary 组件重复存在（独立文件和目录文件）
- 行级过滤逻辑重复（`lib/row-level-filter.ts` 和 `middleware/auth.ts` 各有一份）
- in-memory 缓存无持久化（多实例部署时全部失效）
- 审批系统有 3 种不同实现（旧 ApprovalFlow / 硬编码 LogisticsOrder / 新 ApprovalWorkflow）

### 安全
- CSRF 保护仅在生产环境生效，开发环境完全无保护
- `/api/v1/payments`、`/api/v1/reimbursements` 等 v1 路由无行级数据隔离
- JWT 角色信息嵌入但 7 天不刷新
- 文件上传仅检查 MIME type（可伪造），无魔数检查
- `x-user-id` header 可能被用作认证后门

### 报表
- 全部报表 API 返回 0 值（销售/利润/现金流/采购 数据均为空桩）
- 导出功能未实际实现（xlsx 已安装但 never imported）
- recharts 已安装但无任何页面使用
- 无 YoY/MoM 同比环比分析
- 无数据下钻能力

---

## 🟢 P2 级建议（增强优化）

- 草稿自动保存（新建订单长表单）
- 空态引导（所有列表页"暂无数据"无操作引导）
- 表格列可配置显示 + 列排序
- 出口单证生成（装箱单、报关单）
- 多币种汇率自动获取
- 邮件发送集成
- 超期订单预警
- 报价单过期自动判断
- 无页面过渡动画（登录成功后直接跳转无成就感）
- 自定义报表的数据渲染引擎
- 订阅定时发送调度引擎
- 仪表盘图表用 Recharts/Chart.js 替换手绘 CSS

---

## 📈 流程链路状态总览

```
询盘 ──→ 报价 ──→ 订单 ──→ 生产 ──→ 质检 ──→ 出货 ──→ 物流 ──→ 收款 ──→ 利润核算
 80%     75%      90%       25%      20%      80%      85%      60%        30%
 │        │        │         │         │        │        │         │          │
[弱]     [有]     [强]     [断裂🔴]  [断裂🔴]  [有]     [有]     [弱]      [断裂🔴]
 │        │        │         │         │        │        │         │
 └无一键   └可转    └状态机    └无API    └无API   └可创建   └四级     └缺发票    └样本数据
   转报价   换订单    完整      无页面    无页面    发货记录  审批      无自动     未接入
                                                             核销      真实API
```

---

## 🎯 修复路线图

```
第 1 阶段（优先级最高 — 安全 + 堵漏洞）
  □ /api/roles 加权限检查
  □ 认证系统统一（废弃 auth-simple.ts）
  □ 修复取消订单 Bug
  □ 生产记录 API + 页面
  □ 质检管理 API + 页面

第 2 阶段（打通核心流程）
  □ 利润核算接入真实数据
  □ 发票模块（Invoice model + API + 页面）
  □ alert()/confirm()→Dialog + Toast 统一
  □ 搜索加防抖
  □ 询盘→报价一键转换

第 3 阶段（体验提升 + 报表）
  □ Skeleton 骨架屏
  □ recharts 可视化图表
  □ 报表 API 实现真实数据查询
  □ 导出功能实现
  □ 收款→订单联动

第 4 阶段（持续优化）
  □ 列排序 + 导出
  □ 草稿自动保存
  □ 空态引导
  □ 同比/环比分析
  □ 数据下钻
```

**完成第 1+2 阶段后，系统成熟度预计可提升至 80+。**

---

*报告由 Hermes Agent 使用 `multi-role-process-review` 方法论自动生成*
